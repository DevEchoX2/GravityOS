{
  "name": "gravityos",
  "version": "1.0.0",
  "description": "gravityOS static web application",
  "type": "module",
  "scripts": {
    "dev": "npx -y http-server -p 3000",
    "build": "mkdir -p dist && cp -r assets index.html sw.js dist/ 2>/dev/null || true",
    "lint": "echo 'No lint step required'"
  },
  "dependencies": {}
}
